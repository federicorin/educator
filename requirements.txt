Flask==2.3.3
Flask-SQLAlchemy==3.0.5
Flask-Migrate==4.0.5
Werkzeug==2.3.7
gunicorn==21.2.0

# Base de datos - sqlite3 viene incluido con Python

# Procesamiento de archivos
PyMuPDF==1.23.5
python-docx==0.8.11
python-pptx==0.6.21
Pillow==10.0.0
pytesseract==0.3.10

# Archivos comprimidos
rarfile==4.1
py7zr==0.20.6

# Video y audio
moviepy==1.0.3
SpeechRecognition==3.10.0

# Text to speech
edge-tts==6.1.9

# Markdown y HTML
Markdown==3.5.1
bleach==6.1.0

# Variables de entorno
python-dotenv==1.0.0

# HTTP requests
requests==2.31.0

# Regex (built-in, pero por compatibilidad)
regex==2023.8.8

# JSON (built-in)
# Re (built-in)
# OS (built-in)
# Time (built-in)
# Threading (built-in)
# Signal (built-in)
# Datetime (built-in)
# Pathlib (built-in)
# Secrets (built-in)
# UUID (built-in)
# Asyncio (built-in)
# SMTPLIB (built-in)
# Email (built-in)
# Logging (built-in)
# Typing (built-in)

# Para production
psycopg2-binary==2.9.7
